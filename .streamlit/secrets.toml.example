# Copy this file to secrets.toml for local testing (secrets.toml is gitignored).
# For deployment, set these same keys in Streamlit Cloud: App settings > Secrets.

GROQ_API_KEY = "your-groq-api-key-here"

# This changes every time the Colab notebook hosting Vet Advisor is restarted.
# Update it here (or in Streamlit Cloud secrets) before each demo session.
VETERINARY_ADVISOR_URL = "https://your-ngrok-url.ngrok-free.dev"
